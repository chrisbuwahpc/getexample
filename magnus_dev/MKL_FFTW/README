This example shows how to use MKL for FFTW calls.
It shows both C and Fortran version.

Make sure that you have Intel moduel loaded.

$ module load Intel
or
$ module swap GNU Intel

Set the number of OpenMP threads to 6
$ export OMP_NUM_THREADS=6

To compile and run the test, do
$ make test
